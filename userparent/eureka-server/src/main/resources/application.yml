server:
  port: 7001
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7001/eureka
  #server:
    #enable-self-preservation: false #关闭自我保护
    #eviction-interval-timer-in-ms: 5000 #剔除无效服务的操作时间
spring:
  application:
    name: eureka-server